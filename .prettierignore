# Generated / build artifacts (also in .gitignore, listed here explicitly)
src/generated/
dist/
node_modules/
pnpm-lock.yaml

# Harness prose and state: reformatting these in bulk adds git noise and they
# are owned by humans/leader, not by the formatter. Revisit if desired.
*.md
docs/
progress/
specs/
.claude/
feature_list.json

# Editor-local settings
.vscode/
